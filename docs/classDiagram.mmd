classDiagram
    class Controller {
        -n_clusters: int
        -scaler: StandardScaler
        -kmeans: KMeans
        -pca: PCA
        +dashboardView()
        +cadastroView()
        +analiseView()
        +visualizacaoView()
        +putAthlete(dados: dict)
        +listAthletes(filtros: dict)
        +getCluster(clusterId: str)
        +getModelQuality()
        +getModelMetrics()
        +createCluster(dados: dict)
        +getClusterInfo(clusterId: str)
    }